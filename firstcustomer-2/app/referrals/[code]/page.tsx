import {notFound} from "next/navigation";import {getReferralByCode,query} from "@/lib/db";import {hashToken,safeEqualHex} from "@/lib/security";import {money} from "@/lib/format";export const dynamic="force-dynamic";
export default async function Page({params,searchParams}:{params:Promise<{code:string}>,searchParams:Promise<{key?:string}>}){const{code}=await params,{key}=await searchParams;if(!key)notFound();const auth=await query<{manage_secret_hash:string|null}>("SELECT manage_secret_hash FROM referrals WHERE code=$1",[code]);if(!auth.rows[0]?.manage_secret_hash||!safeEqualHex(auth.rows[0].manage_secret_hash,hashToken(key)))notFound();let r=await getReferralByCode(code);if(!r)notFound();if(r.stripe_account_id&&process.env.STRIPE_SECRET_KEY){try{const {stripe}=await import("@/lib/stripe");const a=await stripe().accounts.retrieve(r.stripe_account_id);const enabled=Boolean(a.payouts_enabled&&a.charges_enabled!==false);if(enabled!==r.payouts_enabled){await query("UPDATE referrals SET payouts_enabled=$2 WHERE id=$1",[r.id,enabled]);r={...r,payouts_enabled:enabled}}}catch{}}
return <main className="narrow page-pad"><div className="page-heading"><span>REFERRER DASHBOARD</span><h1>@{r.x_handle}</h1><p>{r.company_name} · {r.headline}</p></div><div className="stats"><div><span>Clicks</span><strong>{r.clicks}</strong></div><div><span>Approved</span><strong>{r.approved_conversions}</strong></div><div><span>Earned</span><strong>{money(r.earned_cents)}</strong></div><div><span>Paid</span><strong>{money(r.paid_cents)}</strong></div></div>{r.payout_mode==="stripe"?<div className={r.payouts_enabled?"notice":"warning"}>{r.payouts_enabled?"Your Stripe payout account is ready.":"Automatic payout is not ready yet. Complete Stripe onboarding before a reward can be transferred."}</div>:<div className="warning">This campaign uses manual payouts. The company is responsible for paying approved rewards.</div>}{r.payout_mode==="stripe"&&!r.payouts_enabled&&<form action={`/api/referrals/${encodeURIComponent(code)}/payouts?key=${encodeURIComponent(key)}`} method="post"><button className="button primary full">Set up Stripe payouts →</button></form>}<div className="share-strip" style={{marginTop:16}}><span>Referral code</span><code>{r.code}</code></div><p className="fineprint">Keep this dashboard URL private. It controls payout onboarding for this referral.</p></main>}
